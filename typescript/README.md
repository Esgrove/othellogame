# Othello TypeScript

TypeScript implementation using Bun.

## Build and run

Using scripts:

```shell
bun run build
bun othello
bun othello --help
```

Directly with Bun:

```shell
bun src/othello.ts --help
```
